name: build-status

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'zulu'

      - name: setup gradle
        run: |
          git update-index --chmod=+x gradlew
          git config --global user.email "salway.hrishikesh@gmail.com"
          git config --global user.name "Hrishikesh Salway"
          git commit -m "Make gradlew executable"

      - name: Build
        run: ./gradlew build
